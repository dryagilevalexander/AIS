 // private void StreetsRowDoubleClick(object sender, MouseButtonEventArgs e)
       // {
       //     var currentItem = (StreetWithHouses)StreetsGreed.CurrentItem;
       //     int streetId = currentItem.StreetId;
       //     StreetsGreed.Visibility = Visibility.Collapsed;
       //     HousesGreed.ItemsSource = from house in db.Houses.Local
       //                               join apartment in db.Apartments.Local on house.Id equals apartment.HouseId
                                      where (house.StreetId == streetId)
                                      select new
                                      {
                                          HouseId = house.Id,
                                          NumberOfHouse = house.Number,
                                          CountOfApartments = house.Apartments.Count(),
                                          ApartmentsArea = db.Apartments.Local.Where(u => u.HouseId == house.Id).Sum(u => u.Area)
        };
            HousesGreed.Visibility = Visibility.Visible;
        }

        private void HousesRowDoubleClick(object sender, MouseButtonEventArgs e)
        {
        }